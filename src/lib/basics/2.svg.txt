<svg>      
    <defs> 
        <filter id="my-filter" width="100%" height="100%">
            <!-- Blur by 5 px-->
            <feGaussianBlur in="SourceGraphic" stdDeviation="5" />
        </filter>
    </defs>
</svg>